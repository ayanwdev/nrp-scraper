[project]
name = "nrp-scraper"
dynamic = ["version"]
requires-python = ">=3.14"

[tool.poetry]
version = "1.0.0"
packages = [{ include = "main.py" }]

[tool.poetry.scripts]
main = "main:main"

[tool.poetry.dependencies]
feedparser = "^6.0.12"

[dependency-groups]
dev = ["pytest (>=9.0.2,<10.0.0)"]
